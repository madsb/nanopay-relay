{
  "name": "nanopay-relay",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "pnpm --filter @nanopay/relay dev",
    "build": "pnpm -r build",
    "test": "pnpm --filter @nanopay/relay test",
    "compose:up": "docker compose up -d",
    "compose:down": "docker compose down",
    "migrate": "pnpm --filter @nanopay/relay migrate"
  }
}
